1. SETUP DO PC (Frontend - Expo)
Node.js: Baixe e instale a versão LTS em nodejs.org.

Terminal:

Bash
# Criar o app
npx create-expo-app@latest dokkaebi-app --template blank
cd dokkaebi-app

# Instalar libs

npm install styled-components axios lucide-react-native expo-network expo-location expo-constants

npm install --save-dev babel-plugin-styled-components

Ajuste no VS Code: No arquivo babel.config.js, adicione: 

plugins: ['babel-plugin-styled-components'],

📱 2. SETUP DO CELULAR ANTIGO (Backend - Termux)
No Termux:

Bash
# Primeiro o ambiente
pkg update && pkg upgrade
pkg install python termux-api build-essential

# Depois as libs do Python
install -r App/requirements.txt

🌐 3. SETUP DE CONEXÃO E APPS
No Celular Atual (Seu): Instalar Expo Go e Tailscale.

No Celular Antigo (Servidor): Instalar Tailscale e Termux:API (App da Play Store).

No PC: Instalar Tailscale para Windows.


# obs:

# UPGRADE: SISTEMA MULTI-AGENTE
1. No Frontend: Criar um estado (useState) para armazenar o 'currentAgentIP'.
2. No Frontend: Instalar o AsyncStorage para salvar os IPs:
   npx expo install @react-native-async-storage/async-storage
3. No Backend: Manter o código IDÊNTICO em todos os celulares servidores. 
   O que muda é apenas o IP que cada um vai receber do Tailscale.


expo-network (Para pegar informações básicas do Wi-Fi).

react-native-lan-port-scanner (Se quiser algo pronto para varrer IPs).

react-native-ping (Para testar a velocidade com os Agentes).



[ ] PC: Instalar Node.js LTS (nodejs.org)
[ ] PC: Instalar Tailscale e logar
[ ] PC: VS Code + Extensão Python e React Native Tools

[ ] CELULAR ATUAL: Instalar Expo Go
[ ] CELULAR ATUAL: Instalar Tailscale e logar

[ ] AGENTES/HUB: Instalar Termux
[ ] AGENTES/HUB: Instalar Termux:API (App na Play Store)
[ ] AGENTES/HUB: No Termux: rodar 'pkg install termux-api'
[ ] AGENTES/HUB: No Termux: rodar 'termux-setup-storage'
[ ] AGENTES/HUB: Ativar 'Acquire Wake Lock' na notificação

[ ] Criar Lazy Loading para a aba de Ferramentas (não carregar no boot).
[ ] Implementar botão de 'Stop' automático após 30s de escaneamento.
[ ] Adicionar verificação: "Só permitir Scan se estiver no Wi-Fi" (opcional para economizar plano de dados).

[ ] Criar variável global 'SISTEMA_BLOQUEADO' no Hub.
[ ] Implementar Header customizado 'X-Biometric-Auth' no React Native.
[ ] Criar endpoint '/lockdown' em todos os Dokkaebis.

Banco de Dados:

[ ] Criar a tabela notificacoes (global).

[ ] Criar a tabela config_dispositivos (onde guardamos o que cada um pode fazer).

Lógica de Check-in (Schedule):

[ ] Implementar o schedule às 00h e 12h para o Agente se reportar ao banco.

Front-end (React Native):

[ ] Criar um "HOC" (High Order Component) ou um Hook chamado usePermissions que esconde botões baseados no ID do celular.
